#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll N, cnt;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> N;
    queue<ll> q;
    
    for (int i = 0; i <= 9; i++) {
        q.push(i);
    }
    while(!q.empty()) {
        ll x = q.front();
        q.pop();
        if ()
    }
}