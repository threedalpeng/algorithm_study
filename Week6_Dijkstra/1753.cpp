// 최단 경로
#include <iostream>
#include <vector>
#include <queue>

using namespace std;

int V, E, K;

int main()
{
    cin.tie(NULL); cout.tie(NULL);
    ios::sync_with_stdio(false);
    int u, v, w;
    cin >> V >> E;
    cin >> K;
    priority_queue<int> pq;
    
    for (int i = 0; i < E; i++)
    {
        cin >> u >> v >> w;
        
    }
}