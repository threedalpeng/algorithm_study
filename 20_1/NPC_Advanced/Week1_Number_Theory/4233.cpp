#include <iostream>
using namespace std;
using ll = long long;

int fast_power(int a, int p) {
    ll mul = static_cast<ll>(a) * a % p;
}

int main() {
    int p, a;
    while (true) {
        cin >> p >> a;
        if (p == 0 && a == 0)
            break;
    }
}