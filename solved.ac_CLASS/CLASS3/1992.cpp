// 쿼드트리
#include <iostream>
#include <vector>
using namespace std;

int N;
vector<string> vs;

int main()
{
    cin >> N;
    string si, res = "";
    for (int i = 0; i < N; i++)
    {
        cin >> si;
        vs.push_back(si);
    }
    int x = 0, y = 0, n=1;
    while(true)
    {
        
    }
}