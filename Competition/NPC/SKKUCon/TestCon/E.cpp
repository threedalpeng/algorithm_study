#include <bits/stdc++.h>
using namespace std;

int N, M;
int a[100], sum = 0;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> N >> M;

    int rM = floor(sqrt(M));
    for (int i = rM; rM > 0; i--) {
        
    }
}